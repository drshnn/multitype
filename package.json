{
  "name": "multitype",
  "version": "1.0.0",
  "description": "multiplayer typing test website",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "frontend": "cd multitype-client && npm run dev",
    "backend": "cd multitype-server && npm start",
    "dev": "concurrently \"npm run frontend\" \"npm run backend\""

  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
